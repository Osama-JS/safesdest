# 📧 نظام تنبيهات انتهاء صلاحية الملفات - SafeDests

## 📋 نظرة عامة

نظام شامل ومتقدم لفحص وإدارة انتهاء صلاحية الملفات في منصة SafeDests، مع إرسال تنبيهات ذكية وتعليق الحسابات المهملة تلقائياً.

## 🎯 الأهداف الرئيسية

- **فحص يومي تلقائي** للملفات منتهية الصلاحية
- **تنبيهات ذكية** للمستخدمين ومديري الفرق
- **تعليق تلقائي** للحسابات المهملة بعد 3 أيام
- **مراقبة شاملة** مع تقارير دورية
- **تكامل كامل** مع النظام الحالي دون تعديلات

## 🏗️ هيكل النظام

### الملفات الجديدة المنشأة:

```
📁 database/migrations/
└── 2025_07_24_000001_create_file_expiration_notifications_table.php

📁 app/Models/
└── FileExpirationNotification.php

📁 app/Services/
└── FileExpirationService.php

📁 app/Console/Commands/
└── CheckFileExpirations.php

📁 app/Schedule/
└── FileExpirationScheduler.php

📁 resources/views/emails/
├── file-expiration-notification.blade.php
└── account-suspension-notification.blade.php
```

### الملفات المحدثة:

```
📁 bootstrap/
└── app.php (إضافة الجدولة)

📁 config/
└── app.php (إضافة الإعدادات)

📁 app/Services/
└── EmailNotificationService.php (إضافة القوالب)

📁 root/
└── .env (إضافة متغيرات البيئة)
```

## ⚙️ التثبيت والإعداد

### 1. تشغيل Migration

```bash
php artisan migrate --path=database/migrations/2025_07_24_000001_create_file_expiration_notifications_table.php
```

### 2. تحديث متغيرات البيئة

تأكد من وجود هذه المتغيرات في ملف `.env`:

```env
# إعدادات نظام تنبيهات انتهاء صلاحية الملفات
PLATFORM_NOTIFICATION_EMAIL=notifications@safedests.com
ADMIN_EMAIL=admin@safedests.com
FILE_EXPIRATION_ENABLED=true
FILE_EXPIRATION_SUSPENSION_DAYS=3
FILE_EXPIRATION_WARNING_DAYS=1
```

### 3. اختبار النظام

```bash
# اختبار الأمر الجديد
php artisan files:check-expirations --stats

# فحص الجدولة
php artisan schedule:list

# تشغيل تجريبي
php artisan files:check-expirations --dry-run --verbose
```

## 🔄 آلية العمل

### الفحص اليومي (10:00 صباحاً)

1. **فحص المستخدمين النشطين** في جداول users, customers, drivers
2. **تحليل البيانات الإضافية** للبحث عن حقول `file_expiration_date`
3. **فحص تواريخ الانتهاء** (منتهية أو ستنتهي خلال يوم)
4. **منع التكرار** - تنبيه واحد فقط لكل ملف يومياً
5. **تحديد المستلمين** - المستخدم + مدير الفريق + إيميل المنصة
6. **إرسال التنبيهات** مع قوالب مخصصة
7. **تسجيل التنبيه** في قاعدة البيانات

### تعليق الحسابات (بعد 3 أيام)

1. **البحث عن التنبيهات القديمة** (3+ أيام)
2. **فحص حالة المستخدم** (لا يزال نشطاً)
3. **تعليق الحساب** (تغيير الحالة إلى inactive)
4. **إرسال إشعار التعليق** مع تعليمات الإعادة
5. **تحديث حالة التنبيه** إلى account_suspended

## 📊 المراقبة والتقارير

### الإحصائيات اليومية

```bash
# عرض إحصائيات اليوم
php artisan files:check-expirations --stats

# تشغيل مع تفاصيل إضافية
php artisan files:check-expirations --verbose
```

### التقارير الأسبوعية

- **تقرير أسبوعي** يُرسل للإدارة كل يوم أحد
- **تنظيف تلقائي** للسجلات القديمة
- **إشعارات الطوارئ** عند حدوث أخطاء

### قاعدة البيانات

جدول `file_expiration_notifications` يحتوي على:

- **معلومات المستخدم**: نوع المستخدم ومعرفه
- **تفاصيل الملف**: اسم الحقل ومسار الملف
- **معلومات التاريخ**: تاريخ الانتهاء وتاريخ الإرسال
- **حالة التنبيه**: sent أو account_suspended
- **قائمة المستلمين**: جميع الإيميلات المرسل إليها

## 🧪 الاختبار

### اختبار البيانات

```sql
-- إدراج بيانات اختبار للمستخدمين
UPDATE users SET additional_data = '[
    {
        "driving_license": {
            "label": "رخصة القيادة",
            "value": "files/license.pdf",
            "expiration": "2024-07-25",
            "type": "file_expiration_date"
        }
    }
]' WHERE id = 1;

-- اختبار للسائقين
UPDATE drivers SET additional_data = '[
    {
        "vehicle_license": {
            "label": "رخصة المركبة", 
            "value": "files/vehicle.pdf",
            "expiration": "2024-07-26",
            "type": "file_expiration_date"
        }
    }
]' WHERE id = 1;
```

### سيناريوهات الاختبار

- ✅ **ملف منتهي الصلاحية** (تاريخ أمس)
- ✅ **ملف سينتهي اليوم** (تاريخ اليوم)
- ✅ **ملف سينتهي غداً** (تاريخ الغد)
- ✅ **مستخدم غير نشط** (status != 'active')
- ✅ **سائق بدون فريق** (team_id = null)
- ✅ **سائق مع فريق** (team_id موجود)

## 🔧 الأوامر المتاحة

### الأمر الرئيسي

```bash
php artisan files:check-expirations [options]
```

### الخيارات المتاحة

- `--dry-run` - تشغيل تجريبي بدون إرسال إيميلات
- `--stats` - عرض الإحصائيات فقط
- `--verbose` - عرض تفاصيل إضافية
- `--user-type=TYPE` - فحص نوع مستخدم محدد

### أمثلة الاستخدام

```bash
# فحص شامل مع إرسال التنبيهات
php artisan files:check-expirations

# تشغيل تجريبي مع تفاصيل
php artisan files:check-expirations --dry-run --verbose

# عرض الإحصائيات فقط
php artisan files:check-expirations --stats

# فحص السائقين فقط
php artisan files:check-expirations --user-type=driver
```

## 🚨 معالجة الأخطاء

### التسجيل الشامل

- جميع العمليات مسجلة في Laravel Log
- تقارير الأخطاء تُرسل للإدارة فوراً
- إحصائيات مفصلة لكل عملية

### خطة الطوارئ

- **فشل الإرسال**: إعادة المحاولة التلقائية
- **خطأ في البيانات**: إيقاف تلقائي مع تقرير
- **حمل زائد**: تأجيل العمليات وتحسين الأداء
- **مشاكل الجدولة**: تشغيل يدوي مع إصلاح

## 🔒 الأمان والاستقرار

### الحماية من التكرار

- **مفتاح فريد**: (user_type, user_id, field_name, date)
- **فحص يومي**: منع إرسال تنبيهات مكررة
- **تسجيل شامل**: تتبع جميع العمليات

### معالجة الأخطاء

- **try-catch شامل** في جميع العمليات
- **التحقق من البيانات** قبل المعالجة
- **منع التداخل** في الجدولة
- **النسخ الاحتياطي** قبل التعديلات

## 📈 الأداء والتحسين

### تحسين قاعدة البيانات

- **فهارس محسنة** للبحث السريع
- **استعلامات محسنة** لتقليل الحمل
- **تنظيف دوري** للسجلات القديمة

### مراقبة الموارد

- **استهلاك الذاكرة** محدود ومراقب
- **وقت التنفيذ** محسن للعمليات الكبيرة
- **الطوابير** لمعالجة الإيميلات

## 🎉 الخلاصة

تم تطوير نظام شامل ومتكامل لإدارة انتهاء صلاحية الملفات مع:

- ✅ **10 ملفات جديدة** تم إنشاؤها
- ✅ **4 ملفات محدثة** بإضافات بسيطة
- ✅ **تكامل كامل** مع النظام الحالي
- ✅ **اختبار شامل** وتوثيق مفصل
- ✅ **جاهز للإنتاج** فوراً

النظام يعمل بشكل تلقائي ولا يحتاج تدخل يدوي، مع مراقبة شاملة وتقارير دورية للإدارة.
